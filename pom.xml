<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.jaspersoft.jrsctl</groupId>
  <artifactId>jrsctl-parent</artifactId>
  <version>2.2.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>jrsctl</name>
  <description>JasperReports Server lifecycle tool (Jaspersoft)</description>
  <licenses>
    <!-- ADR-0010: one licence, named the same way everywhere; the CycloneDX SBOM copies this onto
         the root component and Phase0SkeletonTest checks the identifier. Text: LICENSE at the root. -->
    <license>
      <name>GPL-3.0-only</name>
      <url>https://www.gnu.org/licenses/gpl-3.0.txt</url>
      <distribution>repo</distribution>
      <comments>GNU General Public License, version 3 only</comments>
    </license>
  </licenses>

  <modules>
    <module>core</module>
    <module>jrs</module>
    <module>ops</module>
    <module>app</module>
    <module>dist</module>
    <module>acceptance</module>
  </modules>

  <properties>
    <maven.compiler.release>21</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <!-- Reproducible archives (jar, shaded jar, zip, tar.gz): every entry carries this timestamp
         instead of the build time, so two builds of the same commit produce the same bytes and a
         signature attests something that can be rebuilt. Bump it with each release. The jlink
         runtime image is not reproducible (ADR-0003), which is why the manifest and the signature
         cover the archive rather than the image alone. -->
    <project.build.outputTimestamp>2026-09-26T01:15:05Z</project.build.outputTimestamp>

    <!-- Runtime dependencies (spec §13.3 approved list) -->
    <picocli.version>4.7.7</picocli.version>
    <jackson.version>2.22.2</jackson.version>
    <slf4j.version>2.0.19</slf4j.version>
    <logback.version>1.5.38</logback.version>
    <logstash-logback.version>7.4</logstash-logback.version>
    <sqlite-jdbc.version>3.53.4.0</sqlite-jdbc.version>
    <json-schema-validator.version>1.4.0</json-schema-validator.version>
    <commons-compress.version>1.28.0</commons-compress.version>
    <!-- transitive through commons-compress; pinned so the SBOM never carries an old lang3 -->
    <commons-lang3.version>3.20.0</commons-lang3.version>
    <semver4j.version>5.8.0</semver4j.version>
    <jline.version>3.30.17</jline.version>

    <!-- Test dependencies -->
    <junit.version>5.14.4</junit.version>
    <assertj.version>3.27.7</assertj.version>
    <wiremock.version>3.13.2</wiremock.version>
    <testcontainers.version>1.21.4</testcontainers.version>
    <jqwik.version>1.9.3</jqwik.version>
    <mockito.version>5.23.0</mockito.version>

    <!-- Build plugins -->
    <errorprone.version>2.50.0</errorprone.version>
    <google-java-format.version>1.22.0</google-java-format.version>
    <spotless.version>2.43.0</spotless.version>
    <surefire.version>3.2.5</surefire.version>
    <versions-plugin.version>2.22.0</versions-plugin.version>
    <surefire.forkCount>1C</surefire.forkCount>
    <surefire.reuseForks>true</surefire.reuseForks>
    <jacoco.version>0.8.15</jacoco.version>
    <!-- No floor by default: app is mostly picocli wiring the acceptance suite drives through the
         real jar, and dist and acceptance produce no coverage of their own. core and ops set
         their own minimum (roadmap item 18). -->
    <jacoco.line.minimum>0.00</jacoco.line.minimum>
    <!-- 9.2.0 (2024) cannot reach today's NVD even with an API key (403); 13.0.0 can. -->
    <dependency-check.version>13.0.0</dependency-check.version>

    <!-- Tests tagged with these JUnit tags are release gates, not phase gates (spec §0 rule 10) -->
    <excluded.groups>needs-jrs,needs-docker</excluded.groups>
  </properties>

  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub Packages</name>
      <url>https://maven.pkg.github.com/robertgorsuch/jrsctl</url>
    </repository>
    <snapshotRepository>
      <id>github</id>
      <name>GitHub Packages Snapshots</name>
      <url>https://maven.pkg.github.com/robertgorsuch/jrsctl</url>
    </snapshotRepository>
  </distributionManagement>

  <dependencyManagement>
    <dependencies>
      <dependency><groupId>com.jaspersoft.jrsctl</groupId><artifactId>jrsctl-core</artifactId><version>${project.version}</version></dependency>
      <dependency><groupId>com.jaspersoft.jrsctl</groupId><artifactId>jrsctl-jrs</artifactId><version>${project.version}</version></dependency>
      <dependency><groupId>com.jaspersoft.jrsctl</groupId><artifactId>jrsctl-ops</artifactId><version>${project.version}</version></dependency>
      <dependency><groupId>com.jaspersoft.jrsctl</groupId><artifactId>jrsctl-app</artifactId><version>${project.version}</version></dependency>

      <dependency><groupId>info.picocli</groupId><artifactId>picocli</artifactId><version>${picocli.version}</version></dependency>
      <dependency><groupId>com.fasterxml.jackson.core</groupId><artifactId>jackson-databind</artifactId><version>${jackson.version}</version></dependency>
      <dependency><groupId>com.fasterxml.jackson.dataformat</groupId><artifactId>jackson-dataformat-yaml</artifactId><version>${jackson.version}</version></dependency>
      <dependency><groupId>com.fasterxml.jackson.datatype</groupId><artifactId>jackson-datatype-jsr310</artifactId><version>${jackson.version}</version></dependency>
      <dependency><groupId>com.fasterxml.jackson.datatype</groupId><artifactId>jackson-datatype-jdk8</artifactId><version>${jackson.version}</version></dependency>
      <dependency><groupId>org.slf4j</groupId><artifactId>slf4j-api</artifactId><version>${slf4j.version}</version></dependency>
      <dependency><groupId>ch.qos.logback</groupId><artifactId>logback-classic</artifactId><version>${logback.version}</version></dependency>
      <dependency><groupId>net.logstash.logback</groupId><artifactId>logstash-logback-encoder</artifactId><version>${logstash-logback.version}</version></dependency>
      <dependency><groupId>org.xerial</groupId><artifactId>sqlite-jdbc</artifactId><version>${sqlite-jdbc.version}</version></dependency>
      <dependency><groupId>com.networknt</groupId><artifactId>json-schema-validator</artifactId><version>${json-schema-validator.version}</version></dependency>
      <dependency><groupId>org.apache.commons</groupId><artifactId>commons-compress</artifactId><version>${commons-compress.version}</version></dependency>
      <dependency><groupId>org.apache.commons</groupId><artifactId>commons-lang3</artifactId><version>${commons-lang3.version}</version></dependency>
      <dependency><groupId>org.semver4j</groupId><artifactId>semver4j</artifactId><version>${semver4j.version}</version></dependency>
      <!-- ADR-0037: line editing and path completion for Prompter.line/path only (issue #102, ADR-0023
           option B). BSD-3-Clause. jline-native carries the platform natives (Windows x86/x86_64/arm64,
           Linux x86/x86_64/arm/arm64/ppc64, FreeBSD), loaded with System.load, not JNA (spec §13.3). -->
      <dependency><groupId>org.jline</groupId><artifactId>jline-terminal</artifactId><version>${jline.version}</version></dependency>
      <dependency><groupId>org.jline</groupId><artifactId>jline-terminal-jni</artifactId><version>${jline.version}</version></dependency>
      <dependency><groupId>org.jline</groupId><artifactId>jline-native</artifactId><version>${jline.version}</version></dependency>
      <dependency><groupId>org.jline</groupId><artifactId>jline-reader</artifactId><version>${jline.version}</version></dependency>

      <!-- Aligns jackson-core and jackson-annotations with the four modules declared above: without
           the BOM they resolve through other libraries' transitive declarations and can lag behind
           databind, which then fails at class-load time (JsonSerializeAs on 2.22 needs 2.22 annotations). -->
      <dependency><groupId>com.fasterxml.jackson</groupId><artifactId>jackson-bom</artifactId><version>${jackson.version}</version><type>pom</type><scope>import</scope></dependency>
      <dependency><groupId>org.junit</groupId><artifactId>junit-bom</artifactId><version>${junit.version}</version><type>pom</type><scope>import</scope></dependency>
      <dependency><groupId>org.assertj</groupId><artifactId>assertj-core</artifactId><version>${assertj.version}</version></dependency>
      <!-- Test-only transitives of WireMock 3.13.2, pinned to patched releases (Dependabot,
           2026-09-14: GHSA-hjcp-jmpx-g3qm httpclient5, httpcore5 below 5.4.3). None of them ships
           in the image. Two advisories cannot be closed this way while WireMock is 3.x, and stay
           test-only: handlebars 4.5 removed helper.NumberHelper, which WireMock 3.13.2 loads, so
           handlebars stays at WireMock's 4.3.1; and the Jetty 11 fixes were never published to
           Maven Central, so Jetty stays at 11.0.26. WireMock 4 (Jetty 12) is still a beta. -->
      <dependency><groupId>org.apache.httpcomponents.client5</groupId><artifactId>httpclient5</artifactId><version>5.6.4</version></dependency>
      <dependency><groupId>org.apache.httpcomponents.core5</groupId><artifactId>httpcore5</artifactId><version>5.4.3</version></dependency>
      <dependency><groupId>org.apache.httpcomponents.core5</groupId><artifactId>httpcore5-h2</artifactId><version>5.4.3</version></dependency>
      <dependency><groupId>org.wiremock</groupId><artifactId>wiremock</artifactId><version>${wiremock.version}</version></dependency>
      <dependency><groupId>org.testcontainers</groupId><artifactId>testcontainers</artifactId><version>${testcontainers.version}</version></dependency>
      <dependency><groupId>org.testcontainers</groupId><artifactId>junit-jupiter</artifactId><version>${testcontainers.version}</version></dependency>
      <dependency><groupId>org.testcontainers</groupId><artifactId>postgresql</artifactId><version>${testcontainers.version}</version></dependency>
      <dependency><groupId>net.jqwik</groupId><artifactId>jqwik</artifactId><version>${jqwik.version}</version></dependency>
      <dependency><groupId>org.mockito</groupId><artifactId>mockito-core</artifactId><version>${mockito.version}</version></dependency>
    </dependencies>
  </dependencyManagement>

  <dependencies>
    <!-- Every module gets the JUnit 5 + AssertJ test stack -->
    <dependency><groupId>org.junit.jupiter</groupId><artifactId>junit-jupiter</artifactId><scope>test</scope></dependency>
    <dependency><groupId>org.assertj</groupId><artifactId>assertj-core</artifactId><scope>test</scope></dependency>
  </dependencies>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
          <configuration>
            <release>21</release>
            <showWarnings>true</showWarnings>
            <compilerArgs>
              <arg>-XDcompilePolicy=simple</arg>
              <arg>--should-stop=ifError=FLOW</arg>
              <!-- Error Prone 2.37+ refuses to run on JDK 21 without it -->
              <arg>-XDaddTypeAnnotationsToSymbol=true</arg>
              <!-- StringConcatToTextBlock is off: the fixtures it flags build scripts and files
                   byte for byte, and a text block turns a batch file's \r\n into \n -->
              <arg>-Xplugin:ErrorProne -XepDisableWarningsInGeneratedCode -XepExcludedPaths:.*/target/generated-.* -Xep:StringConcatToTextBlock:OFF</arg>
              <arg>-Xlint:all,-processing,-serial,-requires-automatic,-requires-transitive-automatic,-missing-explicit-ctor,-try</arg>
              <arg>-Werror</arg>
            </compilerArgs>
            <annotationProcessorPaths>
              <path>
                <groupId>com.google.errorprone</groupId>
                <artifactId>error_prone_core</artifactId>
                <version>${errorprone.version}</version>
              </path>
            </annotationProcessorPaths>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire.version}</version>
          <configuration>
            <excludedGroups>${excluded.groups}</excludedGroups>
            <trimStackTrace>false</trimStackTrace>
            <forkCount>${surefire.forkCount}</forkCount>
            <reuseForks>${surefire.reuseForks}</reuseForks>
            <systemPropertyVariables>
              <!-- review 3.4: a test JVM stays alive after a case that opens a state store, and
                   Windows will not delete the loaded native library, so the copy must land in the
                   build directory rather than inside a @TempDir home. -->
              <org.sqlite.tmpdir>${project.build.directory}</org.sqlite.tmpdir>
            </systemPropertyVariables>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-failsafe-plugin</artifactId>
          <version>${surefire.version}</version>
          <configuration>
            <excludedGroups>${excluded.groups}</excludedGroups>
            <trimStackTrace>false</trimStackTrace>
            <forkCount>${surefire.forkCount}</forkCount>
            <reuseForks>${surefire.reuseForks}</reuseForks>
          </configuration>
        </plugin>
        <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-jar-plugin</artifactId><version>3.4.1</version></plugin>
        <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-shade-plugin</artifactId><version>3.6.2</version></plugin>
        <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-resources-plugin</artifactId><version>3.5.0</version></plugin>
        <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-install-plugin</artifactId><version>3.1.2</version></plugin>
        <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-deploy-plugin</artifactId><version>3.1.2</version></plugin>
        <plugin><groupId>org.apache.maven.plugins</groupId><artifactId>maven-enforcer-plugin</artifactId><version>3.6.3</version></plugin>
        <plugin><groupId>org.codehaus.mojo</groupId><artifactId>exec-maven-plugin</artifactId><version>3.3.0</version></plugin>
        <!-- review 5.1: `mvn versions:display-dependency-updates` and `:display-plugin-updates`
             answer "what is stale?" on a developer machine, without waiting for Dependabot to
             open a pull request. Nothing is ever bumped automatically; the ruleset only keeps
             pre-release versions out of the report. -->
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>versions-maven-plugin</artifactId>
          <version>${versions-plugin.version}</version>
          <configuration>
            <rulesUri>file://${maven.multiModuleProjectDirectory}/build/version-rules.xml</rulesUri>
            <generateBackupPoms>false</generateBackupPoms>
          </configuration>
        </plugin>
        <plugin><groupId>org.codehaus.mojo</groupId><artifactId>build-helper-maven-plugin</artifactId><version>3.6.0</version></plugin>
        <plugin>
          <groupId>com.diffplug.spotless</groupId>
          <artifactId>spotless-maven-plugin</artifactId>
          <version>${spotless.version}</version>
          <configuration>
            <java>
              <googleJavaFormat><version>${google-java-format.version}</version></googleJavaFormat>
              <removeUnusedImports/>
            </java>
          </configuration>
          <executions>
            <execution>
              <!-- validate, not verify: a badly formatted file fails the module in seconds,
                   not after its tests and the shaded jar (ci-fast, 2026-09-19) -->
              <id>spotless-check</id>
              <phase>validate</phase>
              <goals><goal>check</goal></goals>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.jacoco</groupId>
          <artifactId>jacoco-maven-plugin</artifactId>
          <version>${jacoco.version}</version>
          <executions>
            <execution>
              <id>prepare-agent</id>
              <goals><goal>prepare-agent</goal></goals>
            </execution>
            <execution>
              <id>report</id>
              <phase>verify</phase>
              <goals><goal>report</goal></goals>
            </execution>
            <!-- Roadmap item 18: a line-coverage floor on the two modules that decide what
                 happens to an operator's server. The number is a property so each module can
                 state its own and the value is visible in one place; a module that sets
                 jacoco.line.minimum to 0.00 is opting out and has to say why. -->
            <execution>
              <id>check</id>
              <phase>verify</phase>
              <goals><goal>check</goal></goals>
              <configuration>
                <haltOnFailure>true</haltOnFailure>
                <rules>
                  <rule>
                    <element>BUNDLE</element>
                    <limits>
                      <limit>
                        <counter>LINE</counter>
                        <value>COVEREDRATIO</value>
                        <minimum>${jacoco.line.minimum}</minimum>
                      </limit>
                    </limits>
                  </rule>
                </rules>
              </configuration>
            </execution>
          </executions>
        </plugin>
        <plugin>
          <groupId>org.owasp</groupId>
          <artifactId>dependency-check-maven</artifactId>
          <version>${dependency-check.version}</version>
          <configuration>
            <!-- Spec §20: the audit judges what ships, like the SBOM. Test-scope dependencies are
                 skipped; the acceptance module, which ships nothing, sets dependency-check.skip. -->
            <failBuildOnCVSS>7</failBuildOnCVSS>
            <skipTestScope>true</skipTestScope>
            <!-- A Java project: the .NET assembly analyzer needs a dotnet runtime and otherwise ends
                 the run with an initialization error once nothing else has failed. -->
            <assemblyAnalyzerEnabled>false</assemblyAnalyzerEnabled>
            <!-- Bulk NVD data from the project's daily-refreshed mirror (JSON 2.0 feeds): a full load
                 through the REST API took over two hours at its throttled rate. The API key is read
                 by environment-variable name so it never appears on a command line; drop the feed URL
                 (-DnvdDatafeedUrl=) to fall back to the API. -->
            <nvdDatafeedUrl>https://dependency-check.github.io/DependencyCheck_Builder/nvd_cache/nvdcve-{0}.json.gz</nvdDatafeedUrl>
            <nvdApiKeyEnvironmentVariable>NVD_API_KEY</nvdApiKeyEnvironmentVariable>
            <formats>
              <format>HTML</format>
              <format>JSON</format>
            </formats>
          </configuration>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce-toolchain</id>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireJavaVersion><version>[21,)</version></requireJavaVersion>
                <requireMavenVersion><version>[3.9,)</version></requireMavenVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
      </plugin>
      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- Release builds (CI on a v* tag, after versions:set from the tag): refuse a SNAPSHOT version
         and SNAPSHOT dependencies, so a signed release can never be a snapshot renamed by hand
         (spec §20; the v1.0.0 pre-release was exactly that). -->
    <profile>
      <id>release</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-enforcer-plugin</artifactId>
            <executions>
              <execution>
                <id>enforce-release</id>
                <goals><goal>enforce</goal></goals>
                <configuration>
                  <rules>
                    <requireReleaseVersion>
                      <message>a release is built from a release version, never a SNAPSHOT: tag the commit and let CI set the version from the tag (spec §20)</message>
                    </requireReleaseVersion>
                    <requireReleaseDeps>
                      <message>a release must not depend on SNAPSHOT artifacts</message>
                      <onlyWhenRelease>true</onlyWhenRelease>
                    </requireReleaseDeps>
                  </rules>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>dependency-check</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.owasp</groupId>
            <artifactId>dependency-check-maven</artifactId>
            <executions>
              <execution>
                <goals>
                  <goal>check</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
